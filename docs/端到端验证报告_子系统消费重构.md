# geo-reporter 端到端验证报告 —— 子系统消费数据重构

**验证日期**：2026-06-12
**验证 ROI**：黑龙江黑河市逊克县东安金矿（子系统数据最全，含 stru/datacolle/alteration/exploration/geophys/geochem/model3d/drill/insar）
**bbox**：(128.8823, 49.2780, 128.9069, 49.2898)
**产出物**：`/tmp/_e2e_东安验证.docx`（13.9 MB，172 段 / 2 表 / 18 图）、`/tmp/_e2e_东安验证.pptx`（8.6 MB）

## 验证范围说明

本次验证覆盖完整数据链路：**KML 解析 → 地理定位 → 子系统消费融合 → 靶区生成 → 制图 → 文档/PPT 组装 → 读回核对**。

两段 LLM 环节（章节文本提取、综合研判）**未在本次自动验证内**——它们依赖 `claude -p` 子进程，受账户额度与安全策略限制，需经 Web UI 由用户触发。本次用消费层的真实子系统文本作为 mock LLM 输入，验证了 LLM 前后的所有环节，并证明 LLM 所需的输入数据已正确就位。

---

## 一、链路逐环节结果

| 环节 | 结果 | 说明 |
|---|---|---|
| ① KML 解析 | ✅ | 解析东安 .ovkml → area=「东安金矿勘查区范围」，bbox 正确 |
| ② 地理定位 | ✅ | Nominatim 定位成功：黑龙江省 逊克县 |
| ③ 子系统消费融合 | ✅ | 见下表，各章节消费与来源层级正确 |
| ④ 靶区生成 | ✅ | drill 优先生效，6 靶区，坐标合理分散 |
| ⑤ 文档/PPT 组装 | ✅ | docx + pptx 生成，18 图入文 |
| ⑥ 读回核对 | ✅ | 关键内容均在正文（7/8，唯一未命中为 mock 截断假象） |

---

## 二、子系统消费融合（本次重构核心）

| 章节 | texts | 来源层级 | 图件数 | 图件来源 |
|---|---|---|---|---|
| geology | 4 | 子系统本地实证 + 直连API | 7 | **geo-stru + geo-model3d** |
| geography | 3 | 子系统本地实证 + 直连API | 0 | — |
| geophysics | 1 | 子系统本地实证 | 4 | **geo-geophys** |
| geochemistry | 1 | 子系统本地实证 | 0 | — |
| remote_sensing | 1 | 子系统本地实证 | 6 | geo-analyser |
| insar_deformation | 1 | 子系统本地实证 | 0 | — |
| hydrology/climate/infrastructure/mining_rights | 0 | — | 0 | 正式报告由 Tavily+LLM 补全 |

**关键断言（全部 ✅）**：
- ✅ geophysics 主源 = **geo-geophys**，且含欧拉磁源深度（修复了重构前整体漏接）
- ✅ geology 含 **geo-model3d** 建模小节，图件含 **geo-model3d 深度切片**（修复了图件漏接）
- ✅ remote_sensing **已移除深部探测靶区**（错位修正）
- ✅ InSAR **三层合并**：AOI 级形变证据 + 升降轨 2D 分解 + 逐对堆栈（240 对/相干性/基线）
- ✅ geochemistry 含**元素阈值分级表**（背景/弱/中/强）
- ✅ 来源层级由 provider **显式声明**（已删除字符串嗅探）

> 注：hydrology 因 OSM Overpass 当时 504、climate 因 Open-Meteo 沙箱无外网 → 这两章直连 API 当次为空，属环境波动，非代码问题；正式运行联网正常时即有数据，且二者本就有 Tavily 兜底。

---

## 三、靶区生成（geo-drill 主源验证）

- drill 计划孔 **20 个**，exploration 深部靶区 20 个
- ✅ **靶区主源 = geo-drill**（计划孔优先于深部探测）
- ✅ 靶区图生成 6 个靶区，来源标记 = drill
- 靶区坐标跨度：经度 **0.0152°** × 纬度 **0.0092°**（约 1.5km×1km，分布合理）
  - 对比：旧 exploration 前 6 靶区跨度仅 ~10m×20m（曾导致标签重叠、只显示 #6）
- ✅ basemap PNG 正常落盘，标签防重叠生效

---

## 四、文档组装与读回核对

docx：172 段落、2 表格、**18 张内嵌图**（geo-stru/model3d 7 + geo-geophys 4 + geo-analyser 6 + 靶区图 1）。

| 读回核对项 | 结果 |
|---|---|
| InSAR 2D 分解 / 形变速率入正文 | ✅ |
| geo-geophys 欧拉磁源入正文 | ✅ |
| geo-model3d 深度切片图入文 | ✅ |
| 靶区推荐章存在 | ✅ |
| AI 钻探布孔为靶区主源（正文措辞） | ✅ |
| 综合置信评价章存在 | ✅ |
| 钻探/已知矿点验证区 | ✅ |
| InSAR「干涉对」一词入正文 | ❌（见下） |

**表格表头**：表1「靶区 \| 置信等级 \| 中心坐标 \| 评分理由」、表2「评价维度 \| 有利程度 \| 依据」。
仅 2 表为**正确行为**：东安无 deposits 产出 → 无已知矿点表；drill 已作主靶区源 → 去重逻辑正确跳过重复的计划孔附表。

**唯一未命中项说明**：「干涉对」未出现在 mock 正文，原因是验证脚本把每章 summary 截断到 300 字，InSAR 的「干涉对数量: 240 对」排在 2D 分解之后被截掉。消费层完整 InSAR 文本确实含该内容（环节 ③ 已 ✅），正式流程 LLM 拿到的是完整文本，不受影响。**非缺陷**。

---

## 五、本次顺带修复的既有 bug（已验证）

| Bug | 位置 | 状态 |
|---|---|---|
| 图例位置用字符串 `'r'` 而非枚举 | pptx_builder | ✅ 已修，三种图表冒烟通过 |
| `DataLabels` 当可迭代遍历 | pptx_builder | ✅ 已修 |
| 靶区标签重叠仅显示 #6 | basemap | ✅ 已修（聚类扇形散开+引线） |

---

## 六、结论与待办

**结论**：本次「子系统消费数据重构 + 靶区 drill 优先 + InSAR 三层合并」的全部非 LLM 环节端到端验证通过，数据链路完整、各项断言达标。LLM 所需输入数据已正确就位。

**待办（需用户侧）**：
1. LLM 额度恢复后，经 Web UI（http://127.0.0.1:8081）上传东安 KML 跑一次**完整含 LLM** 的报告，确认章节 summary、综合研判、靶区等级覆盖正常。
2. 确认无误后提交（9 个文件改动 + 2 份 docs）。
