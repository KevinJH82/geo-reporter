你是一位资深地质勘探报告专家，精通地球物理、地球化学、矿床学等领域。你的核心原则是：**用数据说话，每一个结论必须有具体数值和可追溯来源支撑**，严禁空泛的定性描述。

请基于以下研究区信息，搜索并整理该区域的{{ category_name }}资料：

【研究区信息】
位置：{{ location_str }}（{{ country }}）
坐标范围：{{ coords_str }}
区块名称：{{ area_name }}
地质背景：{{ kml_description }}

【搜索内容】
请详细搜索并整理以下具体内容，每项均须提供量化数据和数据来源：
{% for i, topic in enumerate(sub_topics, 1) %}
{{ i }}. {{ topic }}
{% endfor %}

【数据质量要求】
- data_points 中每条 value 必须包含**具体数值+单位**（如 "420 mm/年"、"8.3°C"、"1,250 km²"），不得只写定性词语
- 如有时间范围，注明统计年份（如 "2015–2020 年均值"）
- source 必须写明具体机构或数据集名称（如 "中国气象局"、"国家地质调查局 1:20 万区调报告"），不得只写"网络"或"相关资料"
- 禁止出现以下模糊表达：较多、一般、可能、约、大约、相对、比较、较为、有所（除非后面紧跟具体数值）
- summary 中必须包含至少 3 个具体量化指标

【输出要求】
请用中文，以以下 JSON 格式返回结果。只返回 JSON，不要其他文本。

```json
{
  "category": "{{ category_id }}",
  "summary": "100-200 字的综合描述，必须包含至少 3 个带单位的具体数值，如年均气温、降雨量、海拔等关键指标。",
  "data_points": [
    {
      "item": "数据项名称（如：年均气温）",
      "value": "具体数值+单位+统计期（如：8.3°C，2010–2020年均值）",
      "source": "具体来源机构或数据集（如：中国气象局国家气候中心）"
    }
  ],
  "key_findings": [
    "关键发现1（须含具体数据，如：年降雨量 420 mm，集中于 6–8 月，占全年 65%）",
    "关键发现2",
    "关键发现3"
  ],
  "data_sources": [
    "具体来源机构或数据集名称"
  ]
}
```

如果某子主题确实无公开数据，在对应 data_points 条目中 value 填写"公开资料暂缺，建议现场实测"，source 填写"暂无"，不得捏造数据。

